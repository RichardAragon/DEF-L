# Create a secondary player object.
secondary_player = Secondary(position=(0, 0), speed=10, coverage_ability=10, interception_ability=10)

# Calculate the secondary player's coverage area.
coverage_area = secondary_player.calculate_coverage_area()

print(coverage_area)
